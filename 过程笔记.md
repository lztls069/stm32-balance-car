# 平衡车



## mpu6050试用

### 1 配置MX

配置软件I2C: PB3 PB4设置为GPIO输出，调整输出速率和最大速率至high，开漏输出加上拉

### 2 移植文件

复制oled.c oled.h font.c font.h及Mycode文件夹，在MyCode文件夹新建Src Inc文件夹分类。修改Cmakelist，打开浮点数输入。添加头文件。

SSD1306和CH1116存在不同 要修改驱动代码

```c
//oled.c
//原来：
#define OLED_ADDRESS 0x7A
//改成：
#define OLED_ADDRESS 0x78

//OLED_Init()
//改成：最前面加了 200ms 上电延时 无须再多加OLED_Display_On()
void OLED_Init() {
  HAL_Delay(200);                 // 等 OLED 上电稳定

  OLED_SendCmd(0xAE);             // 关显示

  OLED_SendCmd(0x00);             // 列低地址，原来是 0x02
  OLED_SendCmd(0x10);             // 列高地址
  OLED_SendCmd(0x40);             // 起始行
  OLED_SendCmd(0xB0);             // 页地址

  OLED_SendCmd(0x81); OLED_SendCmd(0xFF); // 对比度
  OLED_SendCmd(0xA1);             // 左右方向
  OLED_SendCmd(0xA6);             // 正常显示
  OLED_SendCmd(0xA8); OLED_SendCmd(0x3F); // 64 行
  OLED_SendCmd(0xC8);             // 上下方向
  OLED_SendCmd(0xD3); OLED_SendCmd(0x00); // 显示偏移
  OLED_SendCmd(0xD5); OLED_SendCmd(0x80); // 内部时钟
  OLED_SendCmd(0xD9); OLED_SendCmd(0xF1); // 预充电
  OLED_SendCmd(0xDA); OLED_SendCmd(0x12); // COM 引脚
  OLED_SendCmd(0xDB); OLED_SendCmd(0x20); // VCOMH

  OLED_SendCmd(0x8D); OLED_SendCmd(0x14); // SSD1306 开电荷泵

  OLED_NewFrame();
  OLED_ShowFrame();
  OLED_SendCmd(0xAF);             // 开显示
}

//OLED_ShowFrame() 这一步是防止画面整体右移两列。
//原来：
OLED_SendCmd(0x02);
OLED_SendCmd(0x10);
//改成：
OLED_SendCmd(0x00);
OLED_SendCmd(0x10);
```





### 3 mpu6050使用

pitch（俯仰角）、roll（横滚角）、yaw（偏航角）

- **Pitch**：抬头为正，低头为负。
- **Roll**：向右滚为正，向左滚为负。
- **Yaw**：~~向右偏航为正，向左偏航为负~~ —— **实测相反**（见文末 2026-09-25 记录）：
  这台车上 `gyroz`/`yaw` 为**正**对应**实际左转**，为负对应右转。

| 角度      | 中文名 | 绕哪个轴旋转          | 直观动作      | 典型范围                   |
| :-------- | :----- | :-------------------- | :------------ | :------------------------- |
| **Pitch** | 俯仰角 | 绕 **Y 轴**（横向轴） | 机头抬起/低下 | -90° ~ +90°                |
| **Roll**  | 横滚角 | 绕 **X 轴**（前进轴） | 左右翻滚      | -180° ~ +180°              |
| **Yaw**   | 偏航角 | 绕 **Z 轴**（垂直轴） | 左右转向      | -180° ~ +180° 或 0° ~ 360° |

---

## 转向环整治与调参记录（2026-09-25）

### 最终参数

```c
float Turn_Kp = 0.2, Turn_Kd = 0.02;
int Turn_Out_Max = 40, Turn_Sign = -1;

// Turn_out = Turn_Sign * (Turn_Kp * Target_turn + Turn_Kd * gyroz)，再钳到 ±Turn_Out_Max
```

### 结构改动（原来为什么调不动）

- `Turn_Kd` 一个变量同时当"增益"和"模式开关"（转向时写 0、松手写 0.003），运行期被
  `Control()` 反复改写，注释还自相矛盾。现在它是**常开阻尼增益**，只由调参链路写。
- 转向输出新增独立限幅 `Turn_Out_Max`。原来 `Turn_out` 不限幅，直接加减到左右电机上，
  最后被 ±100 一刀切，直立环会被拖垮。
- 整环符号集中到 `Turn_Sign` 一处，标定时只改这一个数。

### 关键实测结论

1. **被控对象极性**：给 `Turn_out = -23`，100ms 内 `gyroz` 从 +12 冲到 -1137，
   即 `d(gyroz)/dt = p * Turn_out` 中 **p > 0**（p ≈ 400~500 raw/s per PWM）。
2. **`Turn_Sign` 必须为 -1**：`Turn_Kd = +0.003` 配合 p>0 是正反馈。实测 `Turn_Sign=+1`
   时满指令一下去就以 ±12° 剧烈振荡、`Turn_out` 被 ±25 钳死。
3. **方向：`Kp` 与 `Kd` 必须同号**（上面第 1 条定下了 `Turn_Sign*Kd < 0`，剩下由 `Kp` 定方向）。
   稳态偏航角速度 ∝ `-Kp/Kd * Target_turn`，该比值在整体符号翻转下不变，所以"按哪个键往哪转"
   由硬件决定，不能靠翻 `Turn_Sign` 来修。**本车实测 `gyroz` 正 = 实际左转**（与上面 MPU6050
   笔记原先写的相反），因此要"按右键右转"（Target_turn>0 → gyroz<0）就必须 `Kp>0`。
   改 `Kp` 的符号不影响阻尼与稳定性，只改方向；`Kd` 的符号才决定是否正反馈。
4. **小指令无效**：`|Turn_out| < 10` 基本在电机死区内（实测 ±3 差速时编码器几乎为 0）。
   手机 App 按住按键 50ms 内指令就到 150，正常操作不受影响。

### 实测性能（满指令 150、0.72s 脉冲、右转）

- 稳态偏航角速度 37 dps（约 2.4s 转 90°），上升时间 40ms
- 转向中 `roll` 扰动峰峰值约 5°，电机无饱和
- 存在 12~16Hz、幅度约 100% 的偏航角速度振铃

### 未解决：振铃来自平台，不是转向环

把 `Turn_Kd` 设为 0（完全没有偏航反馈）后振铃依旧（10.4Hz、732%，`roll` 同时摆 ±3°，
平衡环公共模 PWM 在 ±15 之间极限环）。即**差速转向本身会削弱平衡环的有效权威**。
要拿到更干净的转向，下一步应该动直立环滚转阻尼 `Vertical_Kd`（当前 -0.0102），
而不是继续加转向环增益。

### 调参链路（SWD，不占串口）

见 `tools/tune/README.md`。`build.ps1` 编译（必须 Release `-Os`，Debug 已占 Flash 93%），
`swd.ps1`/`sweep.ps1` 通过 ST-Link 读写片上调参 mailbox（`Core/Src/tune.c`），
把 100Hz 波形抓进 RAM 再读出来离线算指标。**手机 App 的单字节遥控协议完全没动。**

**注意杜邦线长度**：ST-Link 与小车之间的连线只支持小车单次转 90°，所以每次试验都用
短脉冲（0.6~0.8s ≈ 27°）并左右交替，保证净转角接近 0。
